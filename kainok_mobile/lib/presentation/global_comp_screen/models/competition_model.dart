class CompetitionModel {
  dynamic competitionId;
  dynamic title;
  dynamic categoryTitle;
  dynamic counts;
  dynamic imageLocation;
  dynamic imageId;
  dynamic categoryId;

  CompetitionModel(
      {this.competitionId,
      this.title,
      this.categoryTitle,
      this.counts,
      this.imageLocation,
      this.imageId,
      this.categoryId});
}
