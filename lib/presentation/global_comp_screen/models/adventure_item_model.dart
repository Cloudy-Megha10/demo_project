class AdventureItemModel {
  dynamic categoryName;
  dynamic counts;
  dynamic images;
  dynamic competitionTitle;

  AdventureItemModel(
      {this.categoryName, this.counts, this.images, this.competitionTitle});
}
